SELECT 
    USER_NAME,CLIENT_IP, REPORTED_CLIENT_TYPE,FIRST_AUTHENTICATION_FACTOR, SECOND_AUTHENTICATION_FACTOR, max(event_timestamp) last_login,CLIENT_IP, count(*) login_count
FROM SNOWFLAKE.ACCOUNT_USAGE.LOGIN_HISTORY
